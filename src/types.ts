export interface MenuLink {
  path: string;
  title: string;
}

export interface SiteConfig {
  author: string;
  nickName: string;
  jobPosition: string;
  securizedEmail: string;
  title: string;
}
